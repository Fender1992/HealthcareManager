<GridPopupEditSettings Class="@(Settings.Class)" Height="@(Settings.Height)" MaxHeight="@(Settings.MaxHeight)" MaxWidth="@(Settings.MaxWidth)" MinHeight="@(Settings.MinHeight)" MinWidth="@(Settings.MinWidth)" Title="@(Settings.Title)" Width="@(Settings.Width)">
</GridPopupEditSettings>
<GridPopupEditFormSettings Columns="@(Settings.Columns)" Orientation="@(Settings.Orientation)" ColumnSpacing="@($"{Settings.ColumnSpacing.ToString()}px")"></GridPopupEditFormSettings>
